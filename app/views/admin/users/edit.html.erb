<h1>Edit User</h1>
<p class="pull-right actionbtn"><%= link_to "back to users", admin_users_path, :class => "btn btn-default" %></p>
<div class="clearfix"></div>

<%= form_for(:user, :url => admin_users_path, :html => {:method => :put, :multipart => true, :class => 'form-horizontal', :role => 'form'}) do |f| %>
  <%= render :partial => 'form', :locals => {:f=>f} %>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <button type="submit" class="btn btn-default">Save</button>
    </div>
  </div>
<% end %>

